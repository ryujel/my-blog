# my-blog

마크다운 파일을 읽어서 정적 블로그 웹사이트로 변환하는 프로젝트.

## 목표

- `posts/` 같은 폴더에 있는 마크다운(.md) 글을 읽어 HTML 블로그 페이지로 변환
- 깔끔하고 읽기 좋은 디자인 (본문 가독성 최우선: 적절한 줄 길이, 줄 간격, 여백)
- 다크 모드 지원 (시스템 설정 감지 + 수동 토글)
- 모바일 반응형 (작은 화면에서도 레이아웃/타이포그래피 깨지지 않도록)

## 기술 스택 제약

- **프레임워크 없음.** React, Vue, Next.js, 정적 사이트 제너레이터(11ty, Hugo, Jekyll 등) 사용 금지
- 순수 HTML + CSS + JavaScript(바닐라)만 사용
- 빌드 도구(webpack, vite 등)나 npm 의존성도 가급적 피하고, 필요하면 Node.js 스크립트 정도만 허용
- 마크다운 파싱은 외부 라이브러리 없이 직접 구현하거나, 최소한의 단일 파일 파서를 사용 (전체 방향과 맞는지 사용자와 먼저 상의)

## 디자인 원칙

- 콘텐츠(글) 중심의 미니멀한 레이아웃
- 시스템 폰트 스택 또는 가벼운 웹폰트 하나 정도
- 다크/라이트 모드 모두에서 대비(contrast)와 가독성 확인
- 코드 블록, 인용문, 리스트 등 마크다운 요소의 스타일링 신경 쓰기
- 불필요한 애니메이션/장식 지양 — 읽기 경험을 방해하지 않는 선에서

## 작업 시 참고사항

- 아직 초기 단계 프로젝트이므로 큰 구조 변경 전에는 먼저 방향을 상의할 것
- 새 의존성이나 빌드 도구를 추가하려는 유혹이 생기면, "프레임워크 없이" 원칙에 어긋나지 않는지 먼저 확인
